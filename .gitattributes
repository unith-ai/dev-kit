# Normalize line endings; shell scripts must keep LF to run on macOS and Linux.
* text=auto
*.sh text eol=lf

# Binary assets
*.mp4 binary

# Repository configuration is not part of the downloadable source archive.
.github export-ignore
.gitattributes export-ignore
